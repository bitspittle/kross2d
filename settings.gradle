pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == "kotlin-multiplatform") {
                useModule("org.jetbrains.kotlin:kotlin-gradle-plugin:${requested.version}")
            }
            else if (requested.id.id == "kotlin2js") {
                useModule("org.jetbrains.kotlin:kotlin-gradle-plugin:${requested.version}")
            }
        }
    }
}

rootProject.name = "kross2d"

include(":kross2d-core")
project(":kross2d-core").name = "kross2d"

def exColorCanvas = ":examples:colorcanvas"
include("$exColorCanvas:colorcanvas-common")
include("$exColorCanvas:colorcanvas-jvm")
include("$exColorCanvas:colorcanvas-js")

enableFeaturePreview("GRADLE_METADATA")
